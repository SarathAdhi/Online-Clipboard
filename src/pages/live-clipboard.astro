---
import PageLayout from "@layouts/PageLayout.astro";
import IntroSectionRT from "@modules/RealtimeCb/IntroSectionRT.astro";
import RealtimeCb from "@modules/RealtimeCb";
import { uuid as funcUuid } from "@utils/uuid";

const { url } = Astro;

const id = url.searchParams.get("id");

const uuid = id || funcUuid();

if (!id) {
  return Astro.redirect(`/live-clipboard?id=${uuid}`);
}
---

<PageLayout
  title="Live ClipBoard | 0cb"
  description="Realtime Clipboard to share text between computers very easy."
>
  <RealtimeCb client:load uuid={uuid} />

  <IntroSectionRT />
</PageLayout>
